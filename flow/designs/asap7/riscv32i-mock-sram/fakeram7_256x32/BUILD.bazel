# TODO: mock-openroad creates empty .lib stubs that yosys can't parse
# load("//bazel:orfs.bzl", "orfs_design")

exports_files(glob(["*"]))

filegroup(
    name = "verilog",
    srcs = glob(["*.v", "*.sv"], allow_empty = True),
    visibility = ["//visibility:public"],
)

# orfs_design()
